import Link from "next/link";
export default function AgentDocsPage(){return <main className="min-h-screen bg-background px-4 py-16 text-foreground"><article className="mx-auto max-w-3xl space-y-6"><p className="text-xs font-semibold uppercase tracking-[.18em] text-emerald-500">ProcessForge AI agent API</p><h1 className="text-3xl font-semibold">x402 SOP generation</h1><p className="text-muted-foreground">Send a valid JSON POST to <code>/api/agent/generate-sop</code> with an Idempotency-Key. An unpaid request returns HTTP 402 and a standards-compatible payment requirement. Development mock retries use the advertised mock token; fully configured production requests use the official OKX facilitator on X Layer.</p><section><h2 className="text-lg font-medium">Free endpoints</h2><ul className="mt-2 list-disc space-y-2 pl-5 text-sm text-muted-foreground"><li><code>GET /api/agent</code> — metadata and schemas</li><li><code>GET /api/agent/health</code> — liveness and configuration status</li></ul></section><section><h2 className="text-lg font-medium">Security</h2><p className="mt-2 text-sm text-muted-foreground">Never place API credentials in requests. Payment proofs are verified server-side, request bodies are bounded and validated, and raw payment signatures are not stored.</p></section><Link href="/" className="text-sm text-emerald-400 hover:underline">Return to ProcessForge</Link></article></main>}
