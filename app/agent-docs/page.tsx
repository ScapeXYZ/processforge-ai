import Link from "next/link";
import { getX402Config } from "@/lib/agent/config";

const endpoint = "https://processforgeai.xyz/api/agent/generate-sop";
export default function AgentDocsPage() {
  const payment = getX402Config();
  return <main className="min-h-screen bg-background px-4 py-16 text-foreground"><article className="mx-auto max-w-3xl space-y-8">
    <div><p className="text-xs font-semibold uppercase tracking-[.18em] text-emerald-500">ProcessForge AI agent API</p><h1 className="mt-2 text-3xl font-semibold">x402 SOP generation</h1><p className="mt-3 text-muted-foreground">Production uses the official OKX x402 facilitator on X Layer. Send a valid JSON POST with a unique <code>Idempotency-Key</code>.</p></div>
    <section><h2 className="text-lg font-medium">Production payment</h2><ul className="mt-2 list-disc space-y-1 pl-5 text-sm text-muted-foreground"><li>Endpoint: <code>{endpoint}</code></li><li>Network: <code>eip155:196</code></li><li>Scheme: <code>exact</code></li><li>Asset: <code>{payment.asset || "configured production asset"}</code>{payment.assetAddress ? <> at <code>{payment.assetAddress}</code></> : null}</li><li>Price: <code>{payment.price || "configured amount"}</code> atomic units (smallest-unit integer)</li><li>Recipient: advertised in the HTTP 402 requirement</li></ul></section>
    <section><h2 className="text-lg font-medium">Request</h2><pre className="mt-2 overflow-x-auto rounded-lg bg-muted p-4 text-xs">{`POST /api/agent/generate-sop\nIdempotency-Key: pf-unique-request-001\nContent-Type: application/json\n\n${JSON.stringify({ title: "Supplier invoice approval", description: "Validate each invoice, obtain approval, record evidence, and schedule payment.", industry: "Finance", department: "Accounting", audience: "Accounts payable team", requirements: ["Record approval evidence"], compliance_frameworks: [], output_format: "json" }, null, 2)}`}</pre></section>
    <section><h2 className="text-lg font-medium">Payment flow</h2><p className="mt-2 text-sm text-muted-foreground">A valid unpaid POST returns HTTP 402 with a <code>payment-required</code> header and JSON requirement containing the resource, network, token contract, atomic amount, recipient, and timeout. Use an official x402 buyer to sign that exact requirement, then retry the same body and Idempotency-Key with <code>payment-signature</code>. The server binds the proof to the resource and request hash, verifies it with OKX, blocks replay, and waits for successful settlement before generating or returning content.</p></section>
    <section><h2 className="text-lg font-medium">Successful response</h2><p className="mt-2 text-sm text-muted-foreground">HTTP 200 JSON contains <code>request_id</code>, <code>service</code>, <code>status</code>, <code>sop</code>, <code>analytics</code>, <code>compliance</code>, <code>assumptions</code>, <code>warnings</code>, <code>generated_at</code>, <code>processing_time_ms</code>, and <code>schema_version</code>. A settled response also includes <code>payment-response</code>.</p></section>
    <section><h2 className="text-lg font-medium">Errors</h2><p className="mt-2 text-sm text-muted-foreground"><code>400 INVALID_REQUEST</code>; <code>402 PAYMENT_REQUIRED/PAYMENT_INVALID/PAYMENT_SETTLEMENT_FAILED</code>; <code>409 IDEMPOTENCY_CONFLICT/PAYMENT_REPLAYED/REQUEST_IN_PROGRESS</code>; <code>429 RATE_LIMITED</code>; <code>502 GENERATION_FAILED</code>; <code>503 SERVICE_BUSY/PAYMENT_CONFIGURATION_ERROR</code>; <code>504 AI_TIMEOUT</code>.</p></section>
    <section><h2 className="text-lg font-medium">Security and development</h2><p className="mt-2 text-sm text-muted-foreground">Never send API credentials or private keys to this API. Facilitator credentials remain server-only; raw signatures and private keys are not stored. The deterministic mock uses <code>eip155:1952</code> and advertised mock tokens only outside production. It cannot activate when <code>NODE_ENV=production</code>.</p></section>
    <section><h2 className="text-lg font-medium">Free endpoints</h2><p className="mt-2 text-sm text-muted-foreground"><code>GET /api/agent</code> provides metadata; <code>GET /api/agent/health</code> provides redacted readiness.</p></section>
    <Link href="/" className="text-sm text-emerald-400 hover:underline">Return to ProcessForge</Link>
  </article></main>;
}
