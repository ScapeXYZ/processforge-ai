type InvitationEmailInput={workspaceName:string;inviterName:string;role:string;expiresAt:string;acceptUrl:string};
const escapeHtml=(value:string)=>value.replace(/[&<>'"]/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[char]??char));
export function buildInvitationEmail(input:InvitationEmailInput){const workspace=escapeHtml(input.workspaceName),inviter=escapeHtml(input.inviterName),role=escapeHtml(input.role),url=escapeHtml(input.acceptUrl),expiration=new Intl.DateTimeFormat("en",{dateStyle:"long",timeStyle:"short",timeZone:"UTC"}).format(new Date(input.expiresAt));return{subject:`You’re invited to ${input.workspaceName} on ProcessForge AI`,html:`<!doctype html><html><body style="margin:0;background:#080b12;color:#e5e7eb;font-family:Inter,Arial,sans-serif"><table width="100%" cellpadding="0" cellspacing="0"><tr><td align="center" style="padding:40px 16px"><table width="100%" style="max-width:600px;background:#111722;border:1px solid #253042;border-radius:16px"><tr><td style="padding:32px"><div style="color:#34d399;font-size:13px;font-weight:700;letter-spacing:.12em">PROCESSFORGE AI</div><h1 style="margin:20px 0 12px;color:#fff;font-size:25px">Join ${workspace}</h1><p style="color:#a5b4c7;line-height:1.7">${inviter} invited you to collaborate in <strong style="color:#fff">${workspace}</strong> as <strong style="color:#6ee7b7">${role}</strong>.</p><p style="margin:28px 0"><a href="${url}" style="display:inline-block;background:#34d399;color:#07120e;text-decoration:none;font-weight:700;padding:13px 20px;border-radius:9px">Accept Invitation</a></p><p style="color:#7f8da3;font-size:13px;line-height:1.6">This invitation expires ${expiration} UTC. If you were not expecting it, you can safely ignore this email.</p><p style="color:#64748b;font-size:11px;word-break:break-all">Button not working? ${url}</p></td></tr></table></td></tr></table></body></html>`,text:`ProcessForge AI\n\n${input.inviterName} invited you to join ${input.workspaceName} as ${input.role}.\n\nAccept the invitation: ${input.acceptUrl}\n\nThis invitation expires ${expiration} UTC. If this was unexpected, ignore this email.`}}

